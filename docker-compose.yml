services:
  app:
    build:
      context: .
    restart: on-failure
    env_file: .env
    ports:
      - "${SPRING_LOCAL_PORT}:${SPRING_DOCKER_PORT}"
    networks:
      - app-network

networks:
  app-network:
